{% extends 'base.html.twig' %}

{% block title %}Ajouter un nouvel utilisateur{% endblock %}

{% block body %}
    <div>
        <h1>Ajouter un nouvel utilisateur</h1>

        <div>
            {{ form(form) }}
            <a href="{{ path('app_user_listuser__invoke') }}">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
